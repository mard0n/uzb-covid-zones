import { Zone } from "../types/zone";

export const getZoneBasedOnRef = (subZonesRef: string[], zones: Zone[]) => {
    // return zones.filter(zone => subZonesRef.includes(zone.properties?.refNo))
    return null
} 
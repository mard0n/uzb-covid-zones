import { duOptions } from './du';

const du = {
  type: "du",
  options: ["prepaid", "postpaid", "landline"],
  prepaid: duOptions.prepaid,
  postpaid: duOptions.postpaid,
  landline: duOptions.landline
};

export default du;
